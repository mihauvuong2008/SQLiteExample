<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.freeapp.ngocdong.sqliteexample">

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VIEW.TOPIC_MANAGER_ACTIVITY" />
        <activity android:name=".VIEW.FORM.TUVUNG_MANAGER_ACTIVITY" />
        <activity android:name=".VIEW.FORM.WORD_EDIT_ACTIVITY" />
        <activity android:name=".VIEW.MEAN_MANAGER_ACTIVITY" />
        <activity android:name=".VIEW.TRAIN.GAME_START_Activity"></activity>
    </application>

</manifest>